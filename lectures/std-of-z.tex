\documentclass[12pt]{article}

\usepackage{geometry}
\usepackage{titling}
\usepackage{amsmath,amssymb}
\usepackage{fontspec}
\usepackage{unicode-math}
\usepackage{array}
\usepackage{colortbl}
\usepackage[dvipsnames]{xcolor}
\usepackage[most]{tcolorbox}

\geometry{a4paper, bottom=3cm, left=2.6cm, right=2.5cm}
%\geometry{a4paper, top=2.5cm, bottom=2.5cm, left=2.6cm, right=2.5cm}
\setlength{\droptitle}{-2cm}

\setmonofont{DejaVu Sans Mono}
\setmainfont{Libertinus Serif}[Scale=1.2]
\setsansfont{Libertinus Sans}[Scale=1.19]
\setmathfont{STIXTwoMath-Regular.otf}[Scale=1.2]

\newcommand{\Matrix}[1]{\textbf{#1}}
\newcommand{\Vector}[1]{\textbf{#1}}
\newcommand{\MMat}[1]{\mathbf{#1}}
\newcommand{\MVec}[1]{\mathbf{#1}}
\newcommand{\Norm}[1]{\lVert {{#1}} \rVert}
\newcommand{\NormBeta}[1]{\lVert \beta \mathbf{#1} \rVert}
\newcommand{\NormConst}[1]{\lVert #1 \rVert}
\newcommand{\Trans}{\text{\raisebox{0.5ex}{\scalebox{0.75}{T}}}}
\newcommand{\abs}[1]{\lvert {#1} \rvert}
\newcommand{\Avg}[1]{\mathbf{avg}{(\MVec{#1})}}
\newcommand{\AvgVec}[1]{\mathbf{avg}{(\MVec{#1})}}
\newcommand{\AvgDemeanedVec}[1]{\mathbf{avg}{(\tilde\MVec{#1})}}
\newcommand{\DemeanedVec}[1]{\tilde\MVec{#1}}
\newcommand{\Std}[1]{\mathbf{std}(\MVec{#1})}

\title{Standard Deviation of a Standardized Vector}
\author{Devi Prasad M}
\date{September 2026}

\begin{document}
\pagecolor{yellow!30!brown!40}

\maketitle

Assume $\MVec{x}$ is not a constant vector, so $\Std{x} \neq 0$.
Therefore, the vector $\MVec{z}$, the
\emph{standardized} version of $\MVec{x}$, is well defined.

\begin{align*}
    \MVec{z} &= \dfrac{\DemeanedVec{x}}{\Std{x}} \\[4pt]
             &= \dfrac{\sqrt{n}}{\Norm{\DemeanedVec{x}}} \, \DemeanedVec{x}
                \qquad\qquad \because \, \Std{x} = \dfrac{\Norm{\DemeanedVec{x}}}{\sqrt{n}}
\end{align*}

Since $\MVec{z}$ is a positive scalar multiple of $\DemeanedVec{x}$,
and scaling preserves the average,

\begin{align*}
    \Avg{z} &= \dfrac{\sqrt{n}}{\Norm{\DemeanedVec{x}}} \, \AvgDemeanedVec{x}
             = 0 \qquad\qquad \because \, \AvgDemeanedVec{x} = 0
\end{align*}

Let us try to determine $\Std{z}$.

\begin{align*}
    \Std{z} &= \dfrac{\Norm{\DemeanedVec{z}}}{\sqrt{n}} \\[4pt]
            &= \dfrac{\Norm{z}}{\sqrt{n}} \qquad\qquad \because \, \Avg{z} = 0 \\[4pt]
            &= \dfrac{1}{\sqrt{n}} \left\lVert \dfrac{\sqrt{n}}{\Norm{\DemeanedVec{x}}} \, \DemeanedVec{x} \right\rVert \\[4pt]
            &= \dfrac{1}{\sqrt{n}} \cdot \dfrac{\sqrt{n}}{\Norm{\DemeanedVec{x}}} \cdot \Norm{\DemeanedVec{x}}
                \qquad\qquad \because \, \Norm{c\,\MVec{v}} = |c|\,\Norm{v} \text{ for } c > 0 \\[4pt]
            &= 1
\end{align*}

\end{document}